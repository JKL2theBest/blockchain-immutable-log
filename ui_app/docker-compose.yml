services:
  ui_app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./src:/app/src
      - ../blockchain:/blockchain
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - PYTHONPATH=/app